# AI Lab Exam Evaluation Report

**Student ID:** 10001

**Total Marks:** **12/40**

**Percentage:** **30.00%**

---

## Section-wise Evaluation

| Section | Marks | Evidence | Evaluator Comments |
|---------|------:|----------|--------------------|
| Class Design + Menu Driven | 2/4 | The code has a basic class design with Employee, EmployeeManagement, Program, Reports, and TopKemplo classes. However, the menu-driven functionality is not fully implemented. | The class design is partially correct, but the menu-driven functionality is incomplete. The code does not handle all the menu options correctly. |
| Add Employee + Validation | 4/6 | The EmployeeManagement class has an addEmployee method that checks for duplicate empId and throws a Custom_exception if it already exists. | The validation for adding an employee is correct, but it does not handle other potential errors such as invalid input. |
| Add Bonus + Validation | 0/6 | There is no implementation for adding a bonus or its validation. | The code does not have any implementation for adding a bonus or its validation. |
| Deduct Salary + Validation | 0/6 | There is no implementation for deducting a salary or its validation. | The code does not have any implementation for deducting a salary or its validation. |
| Print All Employees | 2/3 | The Reports class has a printAllEmployees method that prints all employees. | The method to print all employees is correct, but it does not handle potential errors such as an empty list of employees. |
| Display by Department | 1/3 | The Reports class has a printEmployeesByDept method, but it has a logical error in the condition to check the department. | The method to display employees by department is partially correct, but it has a logical error in the condition to check the department. |
| Transaction History | 0/5 | There is no implementation for maintaining or displaying transaction history. | The code does not have any implementation for maintaining or displaying transaction history. |
| Top K Employees | 3/5 | The TopKemplo class has a topkEmployees method that sorts employees by salary and prints the top k employees. | The method to display the top k employees is partially correct, but it does not handle potential errors such as an invalid value of k. |

---

## Overall Feedback

The code has a basic structure, but it lacks complete implementation of the required functionality and has several logical errors. It also does not handle potential errors and exceptions properly.